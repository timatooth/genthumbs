============
genthumbs
============

A command line tool to generate image thumbnails. Also creates a proof sheet in HTML for displaying all the thumbnails on a page.

Usage:
*genthumbs <input folder> [output folder] (default option creates 'output' directory*
